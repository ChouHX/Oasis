# Keep the build context to just what the image needs.
*
!service.py
!entrypoint.sh
!core/
!core/*.py
!requirements.txt
!webui/
!webui/**
!Dockerfile

# the frontend stage runs npm install itself
webui/node_modules/
webui/dist/

# 复制成 .env 并填写。不要提交 .env —— 里面有管理密码。
#
# 这里只放「网页改不了」的东西。代理池、线程数、模式、场次、超时、iCloud 服务、
# Google 出口这些全都在管理界面里配（改完落盘到 /data/oasis_config.json，
# 重启不丢），所以不需要写在这里。
#
#   docker compose pull && docker compose up -d
#   浏览器打开 http://127.0.0.1:8080   （或走 SSH 隧道）

# ---- 必填 --------------------------------------------------------------
# 管理界面密码。不设服务会直接拒绝启动：界面能启停任务、看到账号池、导出凭据，
# 不设密码等于把队列交出去。密码不能在网页上改（鸡生蛋），所以放在这里。
OASIS_WEB_PASSWORD=change-me

# ---- 数据 --------------------------------------------------------------
# 账号池与运行时配置都落在这个目录，跟着 volume 一起持久化。
OASIS_DB=/data/oasis.db

# ---- 以下都是「首次启动的种子」，之后请到网页上改 ---------------------
# 只在配置文件里还没有值时写入。一旦你在网页上改过，就以网页为准 —— 否则每次
# 重启都会把它覆盖回这里。

# 代理池：每行一个，http:// 或 socks5:// 都行。也可以留空，启动后到「代理池」页粘贴。
# OASIS_PROXIES=socks5://user:pass@PROXY_HOST:PROXY_PORT

# 管理界面端口（容器内）。改了要同步 compose 的 ports。
# OASIS_PORT=8080

# 一次跑几个账号。留空则按本机内存和核数自动推荐（2 vCPU / 3GB 算出来是 2）。
# OASIS_THREADS=2

# 场次偏好，逗号分隔，顺序即优先级。
# OASIS_SHOWS=glasgow,manchester,paris

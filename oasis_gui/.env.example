# 复制成 .env 并填写。不要提交 .env —— 里面有管理密码。
#
# 这里只放「网页改不了」的东西。检测节奏（并发、间隔、回看天数）、取件代理、
# iCloud 服务、别名收件箱这些全都在管理界面里配（改完落盘到
# /data/oasis_config.json，重启不丢），所以不需要写在这里。
#
#   docker compose pull && docker compose up -d
#   浏览器打开 http://127.0.0.1:8080   （或走 SSH 隧道）

# ---- 必填 --------------------------------------------------------------
# 管理界面密码。不设服务会直接拒绝启动：界面能启停检测、看到账号池、导出凭据，
# 不设密码等于把账号池交出去。密码不能在网页上改（鸡生蛋），所以放在这里。
OASIS_WEB_PASSWORD=change-me

# ---- 数据 --------------------------------------------------------------
# 账号池与运行时配置都落在这个目录，跟着 volume 一起持久化。
OASIS_DB=/data/oasis.db

# ---- 以下都是「首次启动的种子」，之后请到网页上改 ---------------------
# 只在配置文件里还没有值时写入。一旦你在网页上改过，就以网页为准 —— 否则每次
# 重启都会把它覆盖回这里。

# 管理界面端口（容器内）。改了要同步 compose 的 ports。
# OASIS_PORT=8080

# 同时打开几条收件箱连接。留空则按本机内存和核数自动推荐。
# 同一个 Gmail 收件箱下的 iCloud 别名共用一条连接，所以这是并发连接数，
# 不是「同时读几封信」。
# OASIS_THREADS=2

# 一轮跑完到下一轮开始之间的等待（秒）。中签通知不是秒级事件。
# OASIS_INTERVAL=300

# 首次检测回看多少天。活动已经结束，中签结果很可能早就发出去了，这个窗口
# 要覆盖「结果可能已发」的那段时间；0 = 不设基线，邮箱里所有 Oasis 来信都算数。
# OASIS_LOOKBACK_DAYS=30

# 每个邮箱取最近多少封信来判断。
# OASIS_PER_PAGE=20

# 已中签的账号是否还继续查。默认跳过；想连后续的付款/取票通知一起盯就设 0。
# OASIS_SKIP_HITS=1

# 取件通道（多数网络直连即可，留空）
# OASIS_MAIL_PROXY=socks5://127.0.0.1:1080

# 本地 iCloud 隐藏邮箱服务（在宿主机上跑时用 host.docker.internal）
# OASIS_HME_BASE=http://host.docker.internal:8081
# OASIS_HME_PASSWORD=

# 承载 iCloud 别名的 Gmail 收件箱，导入裸别名时用它补全凭据行
# OASIS_ALIAS_INBOX=someone@gmail.com
# OASIS_ALIAS_PASSWORD=

# 启动时导入的凭据文件（容器内路径）
# OASIS_IMPORT=/data/accounts.txt

# "1" = 跑完一轮就退出，不常驻。
# OASIS_ONESHOT=1
